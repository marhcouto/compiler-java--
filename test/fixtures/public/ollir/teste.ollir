import io;
Arithmetic_mul {

    .construct Arithmetic_mul().V {
        invokespecial(this, "<init>").V;
    }

    .method public static main(args.array.String).V {
        b.bool :=.bool 1.bool;
        c.bool :=.bool 0.bool;
        a.bool :=.bool b.bool ==.bool c.bool;
        invokestatic(io, "print", a.bool).V;

        ret.V;
    }

}
