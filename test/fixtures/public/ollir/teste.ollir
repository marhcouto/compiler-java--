public HelloWorld  {

    .construct HelloWorld().V {
        invokespecial(this, "<init>").V;
    }
    .method public static main(args.array.String).V{
        ret.V;
    }
    .method public testMethod ().i32 {
        t0.array :=.array new(array, 10.i32).array;
        a.array.i32 :=.array.i32 t0.array.i32;
        t1.i32 :=.i32 1.i32;
        a[t1.i32].i32 :=.i32 5.i32;
        ret.i32 1.i32;
    }
}